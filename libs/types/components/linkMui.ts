export type linkMui = {
  activeClassName: string,
  as: string,
  className: string,
  href: string,
  innerRef: (any) => any | Record<string, any>,
  naked: boolean,
  onClick: () => void,
  prefetch: boolean,
};
